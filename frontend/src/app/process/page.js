import ProcessClient from "@/components/process/ProcessClient";

export const metadata = {
  title: "Our Export Process | Eteon International",
  description: "Navigate the complexities of international export with our structured, transparent, and proven workflow. From business inquiry to final dispatch.",
};

export default function ProcessPage() {
  return <ProcessClient />;
}
